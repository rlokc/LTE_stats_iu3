import matplotlib.pyplot as plt

class Visualizer:

    @staticmethod
    def draw_class_scatter(model, features, classifier):
        plt.figure()

        plt.show()

    @staticmethod
    def most_relevant_features(model, set, n = 2):
        return(set[], set[])
